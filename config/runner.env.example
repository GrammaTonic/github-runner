# GitHub Runner Environment Configuration
# Copy this file to runner.env and configure with your settings

# ==========================================
# REQUIRED: GitHub Configuration
# ==========================================

# Your GitHub Personal Access Token
# Create one at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos)
GITHUB_TOKEN=ghp_your_personal_access_token_here

# Your GitHub repository in format: username/repository-name
# Example: GITHUB_REPOSITORY=johndoe/my-awesome-project
GITHUB_REPOSITORY=your-username/your-repo-name

# ==========================================
# OPTIONAL: Runner Configuration
# ==========================================

# Main Runner Configuration
RUNNER_NAME=docker-runner
RUNNER_LABELS=docker,self-hosted,linux,x64

# Chrome Runner Configuration
RUNNER_NAME_CHROME=chrome-runner
RUNNER_LABELS_CHROME=docker,chrome,self-hosted,linux,x64,ui-tests,selenium,playwright

# Runner Group (for GitHub Organizations)
# Leave empty for personal repositories
RUNNER_GROUP=default

# Working Directory inside container
RUNNER_WORKDIR=/home/runner/_work

# Ephemeral Runners (auto-remove after each job)
# Set to true for enhanced security, false for persistent runners
RUNNER_EPHEMERAL=false

# Replace existing runner with same name
RUNNER_REPLACE_EXISTING=true

# ==========================================
# OPTIONAL: Chrome/UI Testing Configuration
# ==========================================

# Virtual Display for headless Chrome
DISPLAY=:99

# Chrome flags for headless operation
CHROME_FLAGS=--headless --no-sandbox --disable-dev-shm-usage --disable-gpu --remote-debugging-port=9222

# ==========================================
# OPTIONAL: Advanced Configuration
# ==========================================

# Container Registry (change if using custom images)
REGISTRY=ghcr.io/grammatonic

# Image versions
RUNNER_IMAGE_TAG=latest
CHROME_IMAGE_TAG=chrome-latest

# Resource Limits (uncomment to enable)
# RUNNER_MEMORY_LIMIT=1g
# RUNNER_CPU_LIMIT=1.0
# CHROME_MEMORY_LIMIT=2g
# CHROME_CPU_LIMIT=1.0

# ==========================================
# EXAMPLES AND COMMON CONFIGURATIONS
# ==========================================

# Example 1: Personal Repository
# GITHUB_TOKEN=ghp_abc123...
# GITHUB_REPOSITORY=johndoe/my-project
# RUNNER_NAME=personal-runner

# Example 2: Organization Repository with Custom Labels
# GITHUB_TOKEN=ghp_xyz789...
# GITHUB_REPOSITORY=myorg/backend-service
# RUNNER_NAME=backend-runner
# RUNNER_LABELS=docker,backend,linux,production
# RUNNER_GROUP=production-runners

# Example 3: UI Testing Setup
# GITHUB_REPOSITORY=myorg/frontend-app
# RUNNER_NAME_CHROME=ui-test-runner
# RUNNER_LABELS_CHROME=docker,chrome,ui-tests,frontend
# CHROME_FLAGS=--headless --no-sandbox --window-size=1920,1080

# Example 4: High-Security Ephemeral Setup
# RUNNER_EPHEMERAL=true
# RUNNER_REPLACE_EXISTING=true
# RUNNER_LABELS=docker,ephemeral,secure
